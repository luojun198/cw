User avatar — shows an image, or initials on a bubble gradient when `src` is absent.

```jsx
<Avatar name="林晚晴" size="lg" />
<Avatar src="/u/12.jpg" name="Ada" />
```

Props: `src`, `name` (initials + alt), `size` (`sm | md | lg` or px number).
