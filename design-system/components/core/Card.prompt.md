Container surface — wrap any grouped content; set `interactive` for clickable cards that lift on hover.

```jsx
<Card interactive onClick={open}>
  <h3>本月支出</h3>
  <p>¥12,480.00</p>
</Card>
```

Props: `interactive` (hover lift), `padding` (px, default 24).
